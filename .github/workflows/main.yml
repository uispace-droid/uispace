import os
import requests
from google.oauth2.credentials import Credentials
from googleapiclient.discovery import build

# بيانات الاعتماد والاتصال ببلوجر
BLOG_ID = os.environ.get('BLOG_ID')
CLIENT_ID = os.environ.get('CLIENT_ID')
CLIENT_SECRET = os.environ.get('CLIENT_SECRET')
REFRESH_TOKEN = os.environ.get('REFRESH_TOKEN')

def main():
    # المصادقة مع Blogger API
    creds = Credentials(
        None,
        refresh_token=REFRESH_TOKEN,
        client_id=CLIENT_ID,
        client_secret=CLIENT_SECRET,
        token_uri="https://oauth2.googleapis.com/token"
    )
    service = build('blogger', 'v3', credentials=creds)

    # جلب الألعاب من مصدر GameMonetize (أو المصدر المستخدم)
    response = requests.get('https://gamemonetize.com/feed.php?format=json')
    games = response.json()

    print(f"تم العثور على {len(games)} لعبة. جارٍ النشر بدون فلترة...")

    # حلقة تكرارية تنشر كل الألعاب بدون استثناء
    for game in games:
        title = game.get('title')
        description = game.get('description', '')
        thumb = game.get('thumb', '')
        game_url = game.get('url', '')

        # تجهيز محتوى المقال مع تضمين اللعبة وصورتها
        html_content = f'''
        <div class="game-container">
            <p>{description}</p>
            <div style="text-align:center; margin:20px 0;">
                <iframe src="{game_url}" width="100%" height="600px" frameborder="0" scrolling="no" allowfullscreen="true"></iframe>
            </div>
        </div>
        '''

        post_body = {
            'title': title,
            'content': html_content,
            'labels': ['Games', game.get('category', 'Arcade')]
        }

        try:
            # إرسال طلب النشر إلى بلوجر
            request = service.posts().insert(blogId=BLOG_ID, body=post_body)
            request.execute()
            print(جتم نشر اللعبة بنجاح: {title})
        except Exception as e:
            print(f"خطأ أثناء نشر اللعبة {title}: {e}")

if __name__ == '__main__':
    main()
